# Deprecated Orchestrator Workspaces

This directory contains legacy per-text workspaces produced by the old orchestrator workflow. They are kept for historical reference only and should not be treated as the canonical source for current text data.

New work should use the current Lyceum text generation and database/release workflow instead of adding or updating folders here.
